import { CryptopunksMetadataHandler } from '../cryptopunks';
import { ethers } from 'ethers';
import { EthereumNetworkType } from '../../ethereum/interface';
describe('Cryptopunks Metadata', () => {
  //TOD
  // it('should get metadata successfully', async () => {
  //   const ethersProvider = new ethers.providers.InfuraProvider(
  //     EthereumNetworkType['mainnet'],
  //     process.env.INFURA_PROJECT_ID,
  //   );
  //   const tokenId = '3102';
  //   const expected_gender = 'Female 2';

  //   const res = await CryptopunksMetadataHandler(tokenId, ethersProvider);

  //   const attributes = res.metadata.attributes;
  //   const gender = attributes.find((attr) => attr.trait_type === 'gender');
  //   expect(gender.value).toBe(expected_gender);
  // });
});
